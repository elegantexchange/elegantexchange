# Local dev — copy to .env
REACT_APP_BACKEND_URL=http://localhost:8000

# Browse the app with seeded boutique mock data (no backend required)
# REACT_APP_UI_ONLY=true

# Production (Vercel) — set in Project → Environment Variables, then redeploy
# REACT_APP_BACKEND_URL=https://your-service.up.railway.app
